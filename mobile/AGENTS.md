# Expo SDK 54

Project is pinned to Expo SDK 54 to match the App Store version of Expo Go.
Docs: https://docs.expo.dev/versions/v54.0.0/

`.npmrc` sets `legacy-peer-deps=true` to work around a peer-dep mismatch
between react-native 0.81.x and @types/react.
